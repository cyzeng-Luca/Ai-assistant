# 本地开发覆盖项 — 仅写和根 .env 不同的值
# 加载顺序: ../.env → .env.local（后加载的覆盖）

# ── 本地地址覆盖（Docker用容器名，本地用localhost）──
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/module_assistant
REDIS_URL=redis://localhost:6380
NODE_ENV=development
